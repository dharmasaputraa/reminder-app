- `init` uses shadcn@3.8.5 (CLI 4.x rejects `-b neutral` — the `-b` flag now means component library).
- The `@reui` registry is configured manually in `components.json` → `https://reui.io/r/{style}/{name}.json`.
- `add @reui/...` resolution was tested: `npx shadcn@3.8.5 view @reui/c-event-calendar-3` succeeds (without installing).
- Event calendar (Task 3): `npx shadcn@3.8.5 add @reui/c-event-calendar-3 --yes`. reUI components use the Base UI API (the `render` prop), so `style` in `components.json` was changed from `new-york` to `base-nova` to make the `src/components/ui/*` primitives use Base UI as well (`@base-ui/react`); with the old style `tsc -b` fails (19 errors on `render`/`asChild`).
- Install output: `src/components/reui/event-calendar/**` (primitives), `src/components/examples/c-event-calendar-3.tsx` (demo, unused — tree-shaken from the bundle), `src/components/ui/*` (base-nova).
- Minimal post-generation adaptation (to keep `tsc -b` green): `process.env.NODE_ENV` → `import.meta.env.DEV` (event-calendar.tsx); remove unused types/imports/destructuring (lib, month-view, resource-view, time-grid, ui/scroll-area).
- The sidebar (shell) is installed from the official shadcn registry (`npx shadcn add sidebar`) — reUI has no sidebar family; it is ported to TanStack Router (`useRouterState` instead of `usePathname`) and Tailwind v4 (`w-(--sidebar-width)`), and the `--color-sidebar*` tokens are already available in base-nova.
- The shell (Task 10) uses the openshadcnblocks `application-shell4` block (`npx shadcn add https://openshadcnblocks.pages.dev/r/block/application-shell4`); the reference lives in `src/components/openshadcnblocks/`, with adaptations in `src/components/app-shell.tsx`. The shadcn sidebar remains available as a primitive (`ui/sidebar.tsx`, `ui/sheet.tsx`, `hooks/use-mobile.ts`) even though the shell does not use it.
- Data grid (contacts master–detail): `pnpm dlx shadcn@latest add @reui/c-data-grid-23 @reui/c-data-grid-20 --yes` installed `src/components/reui/data-grid/**`, `reui/frame.tsx`, `ui/input-group.tsx`, `ui/spinner.tsx`, `hooks/use-copy-to-clipboard.ts` and dep `@tanstack/react-table` (v9 — `useTable` + `features: dataGridFeatures`, NOT the old `useReactTable`). The CLI now updates existing `ui/*` primitives in place (asked to overwrite `checkbox.tsx` — rerun with `--overwrite`). The demo examples stay in `src/components/examples/` as reference; current registry examples already import lucide icons directly (no `IconPlaceholder` anymore). Minimal post-generation adaptation (to keep `tsc -b` green): type-only imports for `ColumnDef`/`PaginationState`/`Row`/`SortingState`, remove unused imports/vars/generics in `data-grid-table*.tsx`. `ui/textarea.tsx` came bundled with the same install (no separate `add textarea` needed). Local adaptation in `data-grid-pagination.tsx`: the page controls always render (upstream hides them when `pageCount <= 1`); arrows self-disable instead.
