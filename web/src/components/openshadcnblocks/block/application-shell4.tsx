import { useState } from "react"
import { ChartColumn, ChevronDown, CircleHelp, LayoutDashboard, ListChecks, Menu, Search, Settings, X } from "lucide-react"

const topSections = ["Overview", "Projects", "Team", "Workspace"]
const pageTabs = [["Dashboard", LayoutDashboard], ["Tasks", ListChecks], ["Roadmap", ChartColumn]]

export function ApplicationShell4() {
  const [active, setActive] = useState("Overview")
  const [activeTab, setActiveTab] = useState("Dashboard")
  const [sheetOpen, setSheetOpen] = useState(false)
  const [profileOpen, setProfileOpen] = useState(false)
  const [mobileSearch, setMobileSearch] = useState(false)

  return <main className="relative flex h-svh min-h-[600px] w-full flex-col overflow-hidden bg-background text-foreground"><header className="sticky top-0 z-20 bg-background"><div className="flex h-14 items-center gap-4 border-b px-4 lg:px-6"><button type="button" aria-label="Open navigation" onClick={() => setSheetOpen(true)} className="flex size-9 items-center justify-center rounded-md hover:bg-muted md:hidden"><Menu className="size-5" /></button><a href="#" className="flex items-center gap-2"><span className="flex size-8 items-center justify-center rounded-sm bg-primary"><img alt="Shadcnblocks" className="size-6 invert dark:invert-0" src="https://deifkwefumgah.cloudfront.net/shadcnblocks/block/logos/shadcnblocks-logo.svg" /></span><b>Shadcnblocks</b></a><nav className="ml-4 hidden items-center gap-1 md:flex">{topSections.map(item => <button type="button" key={item} onClick={() => setActive(item)} className={(active === item ? "bg-muted " : "") + "h-9 rounded-md px-2.5 text-sm font-medium hover:bg-muted"}>{item}</button>)}</nav><div className="ml-auto flex items-center gap-2">{mobileSearch && <input autoFocus className="h-9 min-w-0 flex-1 rounded-md border bg-transparent px-3 text-sm outline-none md:hidden" placeholder="Search..." />}<label className="relative hidden md:block"><Search className="absolute top-1/2 left-2.5 size-4 -translate-y-1/2 text-muted-foreground" /><input className="h-9 w-64 rounded-md border bg-transparent pr-3 pl-8 text-sm outline-none" placeholder="Search..." type="search" /></label><button type="button" aria-label="Search" onClick={() => setMobileSearch(value => !value)} className="flex size-9 items-center justify-center rounded-md hover:bg-muted md:hidden"><Search className="size-5" /></button><div className="relative"><button type="button" onClick={() => setProfileOpen(value => !value)} className="flex h-9 items-center gap-2 rounded-md px-2 hover:bg-muted"><span className="flex size-8 items-center justify-center rounded-full border text-xs">JD</span><span className="hidden text-sm font-medium md:inline">John Doe</span><ChevronDown className="hidden size-3 md:block" /></button>{profileOpen && <div className="absolute top-11 right-0 z-30 w-40 rounded-md border bg-popover p-1 text-sm shadow-lg"><button className="block w-full rounded px-2 py-1.5 text-left hover:bg-muted">Profile</button><button className="block w-full rounded px-2 py-1.5 text-left hover:bg-muted">Sign out</button></div>}</div></div></div><div className="flex h-10 items-center gap-1 border-b bg-muted/30 px-4 lg:px-6">{pageTabs.map(([label, Icon]) => <button type="button" key={String(label)} onClick={() => setActiveTab(String(label))} className={(activeTab === label ? "bg-accent text-accent-foreground " : "") + "flex h-7 items-center gap-1.5 rounded-md px-2.5 text-sm hover:bg-muted"}><Icon className="size-4" />{String(label)}</button>)}</div></header><section className="flex flex-1 p-4 lg:p-6"><div className="min-h-[100vh] flex-1 rounded-xl bg-muted/50 md:min-h-0" /></section>{sheetOpen && <div className="absolute inset-0 z-50 flex md:hidden"><button type="button" aria-label="Dismiss navigation" onClick={() => setSheetOpen(false)} className="absolute inset-0 bg-black/20 backdrop-blur-[1px]" /><aside className="relative z-10 flex h-full w-[calc(100%-2rem)] max-w-[300px] flex-col border-r bg-background p-4 shadow-xl"><div className="flex items-center gap-2"><span className="flex size-8 items-center justify-center rounded-sm bg-primary"><img alt="Shadcnblocks" className="size-6 invert dark:invert-0" src="https://deifkwefumgah.cloudfront.net/shadcnblocks/block/logos/shadcnblocks-logo.svg" /></span><b>Shadcnblocks</b><button type="button" onClick={() => setSheetOpen(false)} className="ml-auto flex size-8 items-center justify-center rounded-md hover:bg-muted" aria-label="Close"><X className="size-4" /></button></div><nav className="mt-12 space-y-2">{topSections.map((item, index) => <div key={item}><button type="button" onClick={() => setActive(item)} className="mb-2 text-xs font-medium tracking-wide text-muted-foreground">{item.toUpperCase()}</button>{index === 0 && <div>{pageTabs.map(([label, Icon]) => <a key={String(label)} href="#" className={(activeTab === label ? "bg-muted " : "") + "flex h-9 items-center gap-2 rounded-md px-2 text-sm"}><Icon className="size-4" />{String(label)}</a>)}</div>}</div>)}</nav><div className="mt-4 border-t pt-4"><p className="mb-2 text-xs font-medium text-muted-foreground">SUPPORT</p><a href="#" className="flex h-9 items-center gap-2 text-sm"><CircleHelp className="size-4" />Help Center</a><a href="#" className="flex h-9 items-center gap-2 text-sm"><Settings className="size-4" />Settings</a></div></aside></div>}</main>
}
